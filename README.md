# pypi-transparency

A proposal for auditable package distribution on Python package indexes.

## Website

The landing page lives in `site/` and is built with [Hugo](https://gohugo.io/).

Run locally:

```bash
cd site
hugo server
```

Build static files:

```bash
cd site
hugo
```

The generated site is written to `site/public/` and is intentionally ignored by git.
